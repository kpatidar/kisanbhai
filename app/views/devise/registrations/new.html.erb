<div class="main_bg">
  <div class="wrap">
    <div class="wrapper">	
      <div class="main">
        <div class="section group">
          <div class="col span_2_of_4">
            <div class="contact-form">
              <h2 class="style">Sign Up</h2>
              <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
                <%= devise_error_messages! %>
                <div>
                  <span><label>Email</label></span>
                  <span><%= f.email_field :email, :autofocus => true, :class => 'text-field' %></span>
                </div>
                <div>
                  <span><label>Password</label></span>
                  <span><%= f.password_field :password, :class => 'text-field' %></span>
                </div>
                <div>
                  <span><label>Password Confirmation</label></span>
                  <span><%= f.password_field :password_confirmation, :class => 'text-field' %></span>
                </div>
                <div>
                  <span><%= f.submit 'Sign Up' %></span>
                </div>
              <% end %>
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
